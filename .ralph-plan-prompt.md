# Ralph Loop Prompt — Audit Remediation

You are an expert Rust systems engineer performing a **production-grade** remediation of critical bugs in a graph database called Lightning. The AUDIT.md file at the repo root contains all findings with file:line references.

## HARD CONSTRAINTS — READ TWICE

1. **PRODUCTION GRADE CODE ONLY** — no stubs, no placeholders, no shortcuts, no `todo!()`, no `unimplemented!()`, no `panic!()` in non-test code, no dead code paths
2. **NEVER SIMPLIFY CODE** — you are forbidden from simplifying, removing, or reducing the complexity of any existing logic. Every fix must be additive/corrective only. If you see code that could be "simpler" — ignore that instinct. Preserve every edge case, every branch, every existing behavior.
3. **NEVER strip features** — if the fix would require commenting out code, removing functionality, or disabling a feature, STOP and reconsider the approach.
4. **You MUST read AUDIT.md fully** before starting any work

## EXECUTION APPROACH

1. Read AUDIT.md from the repo root
2. For each finding, read the affected source file to understand context
3. Apply the fix following the constraints above
4. After each file is changed, verify it compiles: `cargo check 2>&1 | head -50`
5. Do not move to the next finding until the current one compiles cleanly

## FIX ORDER (by severity, top to bottom)

### PHASE 1 — CRITICAL BUGS (fix ALL of these)

1. **lib.rs:1090-1108** — Query plan cache shadowing bug. Remove the second shadowing `let cached_stmt` block. The first block correctly handles cache lookup. Delete lines 1105-1108 entirely.

2. **fusion.rs:5-7** — Cypher injection via `sq()`. Rewrite `sq()` to properly escape both `'` → `''` (Cypher standard double-single-quote escaping) AND backslash `\` → `\\`. Then apply this correctly in ALL call sites: `find_connected_nodes`, `add_observation`, `lookup_node_names`, `materialize_pagerank`, `find_paths`. For `edge_types` in `find_connected_nodes` (line 96-106), apply proper escaping per edge type.

3. **storage/index/csr.rs:222-234,259-274,329-356** — Add `bm.unpin_page()` calls to match every `bm.pin_page()`. Look at how other code in the codebase correctly uses pin/unpin pairs and follow the same pattern.

4. **storage/column.rs:321,1320,1545,2000** — The `stack_buf = [0u8; 64]` risk. Change to use heap allocation with `vec![0u8; element_size]` instead of fixed-size stack buffer, or add a `debug_assert!` / runtime check that `element_size() <= 64`.

5. **storage/index/vector_index.rs:352-354** — Strict aliasing UB. Use `bytemuck::cast_slice` or properly align the cast through a union or `align_to` instead of raw pointer cast from `&[u8]` to `&[f32]`.

6. **physical_plan.rs:189-190** — HashJoin always joins on column 0. The join condition from `LogicalOperator::Join` is in the plan (lines 164-193). Extract the actual equality column indices from the `comparison` field of the Join operator and pass them to `HashJoin::new(left_key, right_key)`.

7. **physical_plan.rs:579** — MERGE discards child operator. Use `let planned_child = self.plan(*child)?;` (remove underscore prefix) and pass `planned_child` to `PhysicalMerge::new(...)`.

8. **processor/operators/aggregate.rs:157-198** — When switching from hash-based to sort-based aggregation, before setting `use_sort_based = true`, flush all accumulated groups from `self.shared_state.groups` into `all_batches` by reconstructing RecordBatch rows from the group map.

9. **parser/mod.rs:114-204** — `strip_modifiers` panics on ORDER BY + SKIP/LIMIT. After modifying `result` (line 147), recompute `upper` or track position adjustments properly. The `adj_return` variable at line 157 is computed but never used — use it to adjust subsequent positions.

10. **storage/wal.rs:495** — Replace `let _computed_crc = digest.finalize()` with validation that compares `_computed_crc` against `stored_crc` and returns an error on mismatch.

11. **processor/operators/dml.rs:119-123,200,528,756** — Move undo record pushes to AFTER the successful write operations. Restructure so UndoRecord::DeleteNode/DeleteRel is only pushed after `batch_append_rows`/`delete_rows` succeeds.

12. **storage/compression/analyzer_test.rs:9,20,31,44,55** — Fix the malformed function calls in all test functions. The double-nested calls with missing parentheses need to be corrected to valid Rust syntax.

### PHASE 2 — HIGH-SEVERITY BUGS

13. **registry.rs:3446-3450** — GEN_RANDOM_UUID: Replace the predictable LCG with a proper CSPRNG (use `getrandom` or `rand::rngs::OsRng`). Fix the UUID format to properly set version=4 and variant bits per RFC 4122.

14. **registry.rs:2688-2693** — RAND function: Move LCG state initialization outside the per-row loop so it produces different values per row. Use `thread_rng()` from `rand` crate.

15. **registry.rs:1787-1797** — DATE_ADD/DATE_SUB MONTH: Fix the control flow. The `checked_add_months` result is assigned but then overridden. Use the correct return from `add_months` and remove the overriding `checked_sub_months` call in the `count < 0` path.

16. **lib.rs:484** — SEARCH function: Change `unwrap()` to proper error propagation with `?`.

17. **transaction/transaction_manager.rs:326-332** — page_merge_locks unbounded growth: Add periodic cleanup of stale entries (e.g., track last access time and remove entries not accessed in N seconds) OR use a bounded LRU cache.

18. **storage_manager.rs:341** — trigram `try_read()` silent skip: Change to `read()` so it blocks instead of silently dropping indexing.

19. **hash_index.rs:163,209,218,458,507-508,589,623** — Use `addr_of!` macro and properly aligned reads instead of direct pointer-to-reference casts.

20. **memory.rs:1241** — u64→f64 precision loss. Use integer formatting directly instead of casting through f64.

21. **lightning/src/types.rs:51-91** — Replace `.unwrap()` with `.ok_or_else(|| LightningError::Type(...))?` in `from_batches`.

22. **lightning/src/connection.rs:168-169,189-190,195** — Use parameterized queries (if the query API supports them) or escape table names by validating they contain only alphanumeric/underscore characters.

### PHASE 3 — HIGH-SEVERITY PERFORMANCE

23. **dml.rs:131-138,380-392,538-539,985-987** — Replace O(N*M²) linear column lookups with a HashMap<String, usize> lookup table built once at the start of each batch loop.

24. **hnsw.rs:233,261** — Avoid cloning entire embeddings array per insert. Clone only the new embedding vector, and for the snapshot needed during search, use a read lock scoped to the search operation.

25. **wasm_function.rs:119-134,150-154,223-227,275-279** — Cache the WASM `Engine` and `Module` across invocations. Move them into the `WasmFunction` struct (or a `OnceCell`/`LazyLock`).

26. **gds/pagerank.rs:68-69, gds/all_shortest_paths.rs:78-80, gds/recursive_join.rs:70** — Replace `vec![0; max_id + 1]` allocations with `HashMap<usize, T>` or `FxHashMap` for sparse node ID spaces.

27. **flatten.rs:42-47** — Batch rows into chunks (e.g., 1024 rows per RecordBatch) instead of creating one RecordBatch per row.

### PHASE 4 — MEDIUM-SEVERITY (HIGH PRIORITY)

28. **column.rs:347-353,1331-1334** — Propagate `mark_row` errors up instead of logging and swallowing. Change the return type of calling functions to `Result`.

29. **buffer_manager.rs:361** — Handle the error from `pf_fh.read_page()` instead of using `let _ =`. Propagate or at minimum invalidate the cached page so garbage data isn't served.

30. **cross_join.rs:78-82** — Change `if let Ok` to propagate the error with `?` instead of silently swallowing.

31. **overflow_file.rs:62-67** — Implement `write_string` properly instead of returning `(0, 0)` stub.

32. **undo_buffer.rs:204,218** — Implement DropConstraint and DropIndex rollback properly instead of logging a warning.

33. **arrow_utils.rs:231** — Use a different sentinel mechanism or length prefix that doesn't conflict with valid UTF-8 byte 0xFF.

34. **arrow_utils.rs:249-252** — Implement proper list-type column handling in `append_raw_to_builder` instead of `return Ok(())`.

35. **memory.rs:1200-1227** — Batch the `MATCH ... SET e.valid_until` updates into a single UNWIND query.

### PHASE 5 — OPTIMIZER STUBS (fix or clean up)

36. **optimizer/foreign_join_pushdown.rs** — Either implement the foreign join pushdown or remove the file. Given NEVER SIMPLIFY constraint, implement it properly: detect foreign key relationships between tables and push joins through them.

37. **optimizer/factorization_rewriter.rs** — Either implement factorization rewriting or remove. Implement: detect shared factors in join graphs and rewrite as factorized plans.

38. **optimizer/projection_pushdown.rs:96** — Fix the `*var = ""` mutation that corrupts expressions. Use a separate set to track remapped indices instead of mutating-in-place.

### VERIFICATION

After each phase, run:
- `cargo check 2>&1 | head -80`
- Fix any compilation errors immediately
- Run `cargo test 2>&1 | tail -30` after all phases to check for regressions

## REMINDER

- NEVER SIMPLIFY CODE
- NEVER strip features
- Production grade only
- Every fix must be correct, complete, and maintain all edge cases
